<div class="card mb-2">
    <h3 class="card-header">{{title}}</h3>
    <div class="card-body">
        <p class="card-text">{{body}}</p>
        <a href="/post/{{id}}" class="stretched-link"></a>
    </div>
    <div class="card-footer text-muted text-end">Posted by: {{user.name}}</div>
</div>